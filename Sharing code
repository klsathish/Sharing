SELECT 
    s.name              AS SchemaName,
    t.name              AS TableName,
    c.column_id         AS ColumnOrder,
    c.name              AS ColumnName,
    TYPE_NAME(c.user_type_id) AS DataType,
    c.max_length        AS MaxLength,
    c.is_nullable       AS IsNullable,
    CASE 
        WHEN pk.column_id IS NOT NULL THEN 'PK'
        WHEN fk.parent_column_id IS NOT NULL THEN 'FK'
        ELSE ''
    END                 AS KeyType,
    fk_details.ReferencedTable,
    fk_details.ReferencedColumn
FROM sys.tables t
JOIN sys.schemas s
    ON t.schema_id = s.schema_id
JOIN sys.columns c
    ON t.object_id = c.object_id
LEFT JOIN (
    -- primary key columns
    SELECT 
        ic.object_id,
        ic.column_id
    FROM sys.indexes i
    JOIN sys.index_columns ic
        ON i.object_id = ic.object_id
       AND i.index_id = ic.index_id
    WHERE i.is_primary_key = 1
) pk
    ON c.object_id = pk.object_id
   AND c.column_id = pk.column_id
LEFT JOIN (
    -- foreign key columns (parent side)
    SELECT 
        fkc.parent_object_id,
        fkc.parent_column_id,
        OBJECT_NAME(fkc.referenced_object_id) AS ReferencedTable,
        c2.name                              AS ReferencedColumn
    FROM sys.foreign_key_columns fkc
    JOIN sys.columns c2
      ON fkc.referenced_object_id = c2.object_id
     AND fkc.referenced_column_id = c2.column_id
) fk_details
    ON c.object_id = fk_details.parent_object_id
LEFT JOIN sys.foreign_key_columns fk
    ON c.object_id = fk.parent_object_id
   AND c.column_id = fk.parent_column_id
ORDER BY s.name, t.name, c.column_id;





List of Documents	|	Contents
Project Scope to Feature Mapping	|	Mapping between scope and features
Web API Document	|	Overview and Introduction: Explain the  API's purpose & main features.
	|	Getting Started: Instructions for initial setup and basic configuration steps.
	|	Authentication and Authorization: Detailed explanation on how to authenticate requests.
	|	API Reference Endpoints: A list of all available API endpoints in Open API Specification(OAS) with
	|	Parameters, Request and Response: must have at least one example schemas for request and response payloads, including data models and their attributes.
	|	Status Codes: Explanation of expected HTTP status codes for various operations (e.g., 200 OK, 400 Bad Request, 500 Internal Server Error).
Technical Design Document	|	Overview and Introduction
	|	System Architecture: A high-level diagram showing the major components and how they interact.
	|	Component Design: Detailed descriptions of individual components, the technologies they use, and their specific functionalities (Frontend, Backend etc.).
	|	Data Design: Details on data structures, database schemas, and how data will be stored and managed.
	|	Interface Design: Descriptions of external interfaces, including user interfaces and APIs.
	|	Implementation and Deployment Plans:
	|	- Implementation Plan
	|	- Deployment & Release Strategy
	|	- Caching & Performance Strategy
	|	- Error Handling & Disaster Management
	|	- Testing strategy
	|	Maintenance Plan: Guidelines for post-deployment activities like bug fixes and updates.
	|	Assumptions and Dependencies: List any assumptions made and other systems or services the project depends on. 
Source Code Document	|	Source code details - repo
	|	Technical Architecture high level diagram
	|	Environment/project setup - should also include IDE/editor configurations.
	|	Project Structure
	|	High level description for the code
	|	Code/Data flow diagram
	|	Database/Other Data source
	|	Application Modules/Features
	|	User access flow diagram
	|	- User Login Flow
	|	- User Access Provisioning
	|	- User Access Mapping with Application Features
SQL- DB Schema & Implementation Document	|	Overview
	|	ERD diagram of DB
	|	DB Details:  Should include all DBO's(Table, Views) details as per the SQL Standard Template
	|	Implementation Details: Details of SP's, View & Functions etc.
Release Document	|	Release identification: Version number, release date, and the document's name.
	|	Overview: A brief summary of the release's purpose and major changes.
	|	New features: A detailed list of new features and functionality.
	|	Fixed issues: A list of bugs and problems that have been resolved.
	|	Known issues: Any remaining bugs or limitations in the release.
	|	Links to related documents like user guides or help manuals.
	|	Other information:
	|	Any disclaimers or important notes.  
Risk Demand Approval	|	
EA Approved Document	|	
Vulnerability Scan approval mail	|	
ISRT Completion Report	|	
SonarQube Scan Report  	|	Auto generated
UAT Approval	|	
Support Cook Book	|	Must include for all components of  Application
	|	1. Common Errors or Bugs Noticed
	|	2. Frequent Queries from End Users
	|	3. Log all production bugs and categorize
App & Error Monitoring	|	
RCA's	|	
Post Go Live - Project Handover Session & Approval	|	
